[user]
    name = Noé Sánchez
    email = noeszc@gmail.com

[includeIf "gitdir:~/code/work/"]
    path = ~/.work.gitconfig

[url "git@github.com:noeszc/"]
    insteadOf = "me:"

[core]
    pager = delta
    sshCommand = ssh -i ~/.ssh/id_ed25519 -F /dev/null

[delta]
    line-numbers = true

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[push]
    autoSetupRemote = true
